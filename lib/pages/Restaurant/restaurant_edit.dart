import 'package:flutter/material.dart';

class RestaurantEdit extends StatelessWidget {
  const RestaurantEdit({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
